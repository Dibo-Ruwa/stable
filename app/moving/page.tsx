import Moving from "@/containers/newMoving";

export const metadata = {
  title: "Moving and Delivery Services",
};

const page = () => {
  return (
    <div>
      <Moving />
    </div>
  );
};

export default page;
