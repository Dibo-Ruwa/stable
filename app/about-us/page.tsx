import {AboutUs} from "@/containers/about-us/AboutUs";

export const metadata = {
  title: "About",
};

const page = () => {
  return (
    <div>
      <AboutUs />
    </div>
  );
};

export default page;
